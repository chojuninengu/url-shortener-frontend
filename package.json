{
  "name": "url-shortener-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.8.3",
    "cra-template-tailwindcss": "4.0.1",
    "gh-pages": "^6.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-scripts": "5.0.1",
    "react-toastify": "^11.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
jq '. + { "homepage": "https://chojuninengu.github.io/url-shortener-frontend", "scripts": { "predeploy": "npm run build", "deploy": "gh-pages -d build" } }' package.json > package.tmp && mv package.tmp package.json